/* Logica prodotti in evidenza*/var slideIndex = 1;showSlides(slideIndex);function plusSlides(n) {	showSlides(slideIndex += n);}function currentSlide(n) {	showSlides(slideIndex = n);}function showSlides(n) {	var i;	var slides = document.getElementsByClassName("mySlides");	var dots = document.getElementsByClassName("dot");	if (n > slides.length) {slideIndex = 1}	if (n < 1) {slideIndex = slides.length}	for (i = 0; i < slides.length; i++) {		slides[i].style.display = "none";	}	for (i = 0; i < dots.length; i++) {		dots[i].className = dots[i].className.replace(" active", "");	}	slides[slideIndex-1].style.display = "block";	dots[slideIndex-1].className += " active";}/* logica prodotti scontati */let slideIndexSconti = 1;showSlidesSconti(slideIndexSconti);function plusSlidesSconti(n) {    showSlidesSconti(slideIndexSconti += n);}function showSlidesSconti(n) {    let i;    let slides = document.getElementsByClassName("mySlidesSconti");    // Interrompe se la pagina non ha l'elemento (es. su altre pagine)    if (slides.length === 0) {        return;    }    if (n > slides.length) { slideIndexSconti = 1 }    if (n < 1) { slideIndexSconti = slides.length }    for (i = 0; i < slides.length; i++) {        slides[i].style.display = "none";    }    slides[slideIndexSconti - 1].style.display = "block";}